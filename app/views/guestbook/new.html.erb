<div class="max-w-3xl mx-auto">
  <div class="bg-white border border-gray-200 rounded-lg p-8">
    <div class="mb-6 pb-6 border-b border-gray-200">
      <h1 class="text-2xl font-semibold text-gray-900 mb-2">Написать сообщение</h1>
      <p class="text-gray-600">Заполните форму ниже, чтобы оставить запись в гостевой книге</p>
    </div>

    <form action="/guestbook" method="post" class="space-y-5">
      <%= csrf_meta_tags %>

      <div>
        <label for="name" class="form-label">
          Ваше имя
        </label>
        <input
          type="text"
          name="name"
          id="name"
          required
          class="form-input"
          placeholder="Введите ваше имя"
        />
      </div>

      <div>
        <label for="email" class="form-label">
          Email
        </label>
        <input
          type="email"
          name="email"
          id="email"
          required
          class="form-input"
          placeholder="your@email.com"
        />
      </div>

      <div>
        <label for="message" class="form-label">
          Сообщение
        </label>
        <textarea
          name="message"
          id="message"
          rows="8"
          required
          class="form-input resize-y"
          placeholder="Напишите ваше сообщение здесь..."
        ></textarea>
      </div>

      <div class="flex gap-4">
        <button type="submit" class="btn btn-primary flex-1">
          Отправить сообщение
        </button>
        <%= link_to "Отмена", guestbook_path, class: "btn btn-secondary flex-1" %>
      </div>
    </form>
  </div>
</div>
