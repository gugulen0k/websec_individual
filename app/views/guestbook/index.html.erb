<div class="max-w-5xl mx-auto space-y-6">
  <div class="bg-white border border-gray-200 rounded-lg p-8">
    <div class="flex items-center justify-between mb-6 pb-6 border-b border-gray-200">
      <div>
        <h1 class="text-2xl font-semibold text-gray-900">Гостевая книга</h1>
        <p class="text-gray-600 mt-1">Оставьте своё сообщение</p>
      </div>
      <%= link_to guestbook_new_path, class: "btn btn-primary" do %>
        <span class="inline-flex items-center gap-2">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
          </svg>
          Написать сообщение
        </span>
      <% end %>
    </div>

    <% if @messages.empty? %>
      <div class="text-center py-12">
        <div class="text-5xl mb-4">📝</div>
        <p class="text-lg text-gray-700 mb-2">Пока нет сообщений</p>
        <p class="text-gray-500">Станьте первым, кто оставит запись</p>
      </div>
    <% else %>
      <div class="space-y-4">
        <% @messages.each do |msg| %>
          <div class="bg-gray-50 border border-gray-200 rounded-lg p-6 hover:border-gray-400 transition-colors">
            <div class="flex items-start justify-between mb-4 pb-4 border-b border-gray-200">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-gray-700 rounded-full flex items-center justify-center text-white font-medium text-base">
                  <%= msg.name[0].upcase %>
                </div>
                <div>
                  <p class="font-medium text-gray-900"><%= h msg.name %></p>
                  <p class="text-sm text-gray-600"><%= h msg.email %></p>
                </div>
              </div>
              <div class="text-right">
                <p class="text-sm text-gray-500">
                  <%= msg.created_at.strftime("%d.%m.%Y") %>
                </p>
                <p class="text-xs text-gray-400">
                  <%= msg.created_at.strftime("%H:%M") %>
                </p>
              </div>
            </div>

            <div class="prose prose-sm max-w-none text-gray-700 leading-relaxed">
              <%= msg.message.html_safe %>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
