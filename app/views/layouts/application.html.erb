<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Web Security Individual Work" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Tailwind CSS %>
    <script src="https://cdn.tailwindcss.com"></script>
    
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <% if current_user %>
      <nav class="bg-white border-b border-gray-200 sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center h-16">
            <div class="flex items-center">
              <h1 class="text-xl font-semibold text-gray-900">
                <span class="hover:text-gray-700 transition-colors">Security App</span>
              </h1>
            </div>

            <div class="flex items-center gap-6">
              <span class="text-sm text-gray-600">
                Привет, <span class="font-medium text-gray-900"><%= current_user.login %></span>
              </span>

              <div class="flex gap-4">
                <% if session[:role] == "admin" %>
                  <%= link_to "Админ-панель", admin_dashboard_path, class: "text-sm text-gray-700 hover:text-gray-900 transition-colors" %>
                <% elsif session[:role] == "manager" %>
                  <%= link_to "Панель менеджера", manager_dashboard_path, class: "text-sm text-gray-700 hover:text-gray-900 transition-colors" %>
                <% else %>
                  <%= link_to "Личный кабинет", user_dashboard_path, class: "text-sm text-gray-700 hover:text-gray-900 transition-colors" %>
                <% end %>

                <%= link_to "Гостевая книга", guestbook_path, class: "text-sm text-gray-700 hover:text-gray-900 transition-colors" %>

                <%= button_to "Выйти", logout_path, method: :delete,
                    class: "text-sm text-gray-600 hover:text-gray-900 transition-colors",
                    form: { class: "inline" } %>
              </div>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <main class="<%= current_user ? 'py-8' : '' %>">
      <div class="<%= current_user ? 'max-w-7xl mx-auto px-4 sm:px-6 lg:px-8' : '' %>">
        <% flash.each do |key, message| %>
          <div class="alert alert-<%= key == 'alert' ? 'error' : key == 'notice' ? 'notice' : 'success' %>">
            <%= message %>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </main>

    <% unless current_user %>
      <footer class="fixed bottom-0 w-full bg-white border-t border-gray-200 py-4">
        <div class="max-w-7xl mx-auto px-4 text-center text-sm text-gray-500">
          &copy; 2025 Web Security App
        </div>
      </footer>
    <% end %>
  </body>
</html>
